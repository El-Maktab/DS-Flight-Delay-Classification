# DS-Flight-Delay-Classification

-   To run the project till the last implemented phase, simply download the flights data from this [link](https://www.kaggle.com/datasets/usdot/flight-delays), then add the `flights.csv` to `data/raw`, then add the rest of the `.csv` files to the `data/external`. After that run the `run.sh` script.